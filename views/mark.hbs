<section id="mark-success">
  <p>Just testing: url is {{url}}</p>

  <p>Saved your place in
    {{#each updatedDogears}}
      {{#if this.display_name}}{{this.display_name}}{{else}}{{this.prefix}}{{/if}}{{#unless @last}}, {{/unless}}
    {{/each}}
  </p>

  <div id="countdown" data-returnto="{{url}}">

  </div>
</section>
